{
  "name": "@mylib/hooks",
  "version": "1.0.0",
  "description": "@mylib hooks library",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && rollup -c rollup.config.mjs",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^12.1.1",
    "tslib": "^2.8.1"
  },
  "peerDependencies": {
    "vue": "^3.5.13"
  }
}
