{
  "name": "@mylib/build",
  "description": "glup build config",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && gulp -f ./gulpfile.cjs",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "devDependencies": {
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0",
    "gulp": "^5.0.0",
    "gulp-babel": "^8.0.0",
    "gulp-clean": "^0.4.0",
    "gulp-typescript": "6.0.0-alpha.1",
    "gulp-uglify": "^3.0.2"
  }
}
